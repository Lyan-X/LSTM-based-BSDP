# 基于深度学习的城市共享单车调度需求预测与运维管理平台 项目启动与部署文档

## 1. 环境准备

- Python 3.9+
- Django 4.2+
- TensorFlow 2.10.0
- Windows + CUDA 11.2 + cuDNN 8.1

## 2. 安装依赖

```powershell
pip install -r requirements.txt
```

## 3. 数据初始化

```powershell
python manage.py makemigrations
python manage.py migrate
python manage.py loaddata station_master_fixture.json
python manage.py loaddata station_hourly_data_fixture.json
```

## 4. 模型资产

训练完成后，模型资产固定存放于：

`bike_dispatch_platform/demand_prediction/model_assets/`

包含：

- `bike_sharing_lstm_model.keras`
- `scaler.pkl`
- `model_metrics.json`
- `training_loss_curve.png`
- `sample_prediction_curve.png`

## 5. GPU 校验

```powershell
$cuda='C:\\Program Files\\NVIDIA GPU Computing Toolkit\\CUDA\\v11.2'
$bin=Join-Path $cuda 'bin'
$libnvvp=Join-Path $cuda 'libnvvp'
$env:CUDA_PATH=$cuda
$env:CUDA_HOME=$cuda
$env:Path="$bin;$libnvvp;$env:Path"
python -c "import tensorflow as tf; print(tf.__version__); print(tf.config.list_physical_devices('GPU')); print(tf.test.is_built_with_cuda())"
```

## 6. 项目启动

```powershell
python manage.py runserver
```

访问路径：

- `/system/dashboard/`
- `/predict/api/48h/`
- `/predict/api/station/1/`
- `/operation/`

## 7. 常见问题

- 若 TensorFlow 未识别 GPU，请先确认 CUDA 11.2 与 cuDNN 8.1 的 DLL 已落地到 `CUDA\v11.2\bin`
- 若数据库锁定，请停止重复运行的 Python/Django 进程后重试
- 若页面样式异常，请优先检查静态资源 CDN 网络连通性
