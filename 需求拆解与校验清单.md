# 基于《软件需求规格说明书 V3.2》的需求拆解与校验清单

## 1. 校验范围与说明
- 校验主线：`bike_dispatch_platform`
- 校验依据：`软件需求规格说明书 V3.2`
- 最高约束：第 7 章“项目不可变更核心红线”
- 当前状态定义：
  - `已实现达标`：代码、页面、接口与自动化校验均已满足要求
  - `已补全待终验`：代码与脚本已补齐，但仍需执行类验收才能形成最终结论
  - `不纳入当前主线验收`：遗留兼容入口存在，但不作为本次 SRS 主线验收对象

## 2. 红线校验
| 编号 | 需求名称 | 验收标准 | 红线约束 | 当前状态 | 证据/说明 |
| --- | --- | --- | --- | --- | --- |
| REDLINE-01 | 62 个固定停车点不可变更 | 站点数量固定 62，站点主数据不允许增删改 | 红线 1 | 已实现达标 | 主数据由 `station_info.master_data` 与 `sync_parking_spots()` 固化；自动化校验结果 `active_station_count=62` |
| REDLINE-02 | 数据源不可变更 | 仅使用华盛顿真实订单 1:1 映射后的燕大 62 站点核心数据集 | 红线 2 | 已实现达标 | 预测服务、训练脚本、运行时服务统一读取核心数据集 |
| REDLINE-03 | 全局车辆守恒不可突破 | 任意时刻全局车辆总数固定 1200 | 红线 3 | 已实现达标 | `runtime_service`、`vehicle_service`、手动调度校验链均以 1200 为硬约束 |
| REDLINE-04 | 时序处理逻辑不可更改 | 48→48 滑窗、7:2:1 时间切分、`shuffle=False` | 红线 4 | 已实现达标 | 现有 `build_lstm_system.py` 与并行 `build_lstm_system_srs.py` 均遵循该口径 |
| REDLINE-05 | 现有 UI 与地图特色不可更改 | 不改布局、地图样式、热力逻辑与图表语言 | 红线 5 | 已实现达标 | 本轮仅增强热力图数据视图与车辆运维页面，不改既有视觉语言 |
| REDLINE-06 | 现有生产闭环不可破坏 | 10 秒刷新、SQLite 串行锁、后端接口、生产模型与归档文档不可破坏 | 红线 6 | 已实现达标 | 未修改 `runtime_service.py` 与 `station_prediction_service.py` 核心逻辑，仅做调用层适配 |
| REDLINE-07 | 技术中立性不可变更 | 允许并行模型优化，不限定单一算法实现 | 红线 7 | 已实现达标 | 保留并行 SRS 训练脚本，不覆盖生产模型 |

## 3. 功能性需求校验
| 编号 | 需求名称 | 验收标准 | 对应红线 | 当前状态 | 证据/说明 |
| --- | --- | --- | --- | --- | --- |
| REQ-DATA-01 | 核心数据集读取 | 系统只读接入映射后的 62 站点小时级数据 | REDLINE-02 | 已实现达标 | 预测服务、运行时服务、数据管理页统一读取核心数据集 |
| REQ-DATA-02 | 天气数据左连接能力 | 保留天气关联结果，不允许改原始映射逻辑 | REDLINE-02 | 已实现达标 | 核心数据集构建阶段已完成映射；运行时不开放破坏性上传入口 |
| REQ-DATA-03 | 特征工程补全 | 包含 `hour_of_day`、`day_of_week`、`is_holiday` | REDLINE-04 | 已补全待终验 | 新增 `calendar_features.py` 与 `build_lstm_system_srs.py`；并行 SRS 资产尚未实际训练 |
| REQ-PRED-01 | 48→48 预测输出 | 接口返回 48 小时净流量，保留库存轨迹 | REDLINE-04 | 已实现达标 | `/predict/api/48h/` 返回 62 站点 × 48 小时预测结果 |
| REQ-PRED-01A | T+1 作为唯一业务决策依据 | 调度仅使用第 1 个小时预测值 | REDLINE-04 | 已实现达标 | `runtime_service` 已由“未来 4 小时”收敛为 `T+1` 缺口计算 |
| REQ-PRED-01B | T+1 非零净流量准确率 ≥85% | 作为模型验收主指标 | REDLINE-04 | 已补全待终验 | 并行训练脚本已支持 T+1 指标；待实际训练并核验 |
| REQ-PRED-02 | 时序切分 | 7:2:1、测试集固定最靠后时间段 | REDLINE-04 | 已实现达标 | `build_lstm_system.py` 与 `build_lstm_system_srs.py` 均按时间切分 |
| REQ-PRED-03 | 预测结果展示 | 前端展示 48 小时趋势、T+1 重点信息、核心指标 | REDLINE-05 | 已实现达标 | 预测页、站点预测页、结果页已补齐 T+1 字段与导出入口 |
| REQ-PRED-04 | 生产模型并行扩展 | 不能覆盖 `bike_sharing_lstm_model.keras/scaler.pkl` | REDLINE-06 | 已实现达标 | 新增 `*_srs` 并行资产路径，原生产资产不变 |
| REQ-OP-01 | 自动调度闭环 | 调度基于 T+1 缺口 + 运输问题最优求解 + 1200 守恒 | REDLINE-03/04 | 已实现达标 | `runtime_service` 保留线性规划求解，并将决策口径切至 T+1 |
| REQ-OP-02 | 热力图与地图 | 固定 62 站点、固定地图样式、红黄绿状态展示 | REDLINE-01/05 | 已实现达标 | 地图仍使用原有 62 点位、颜色和直接数字标记 |
| REQ-OP-03 | 手动调度 | 起点库存、终点容量、1200 守恒三重校验 | REDLINE-03 | 已实现达标 | `create_manual_dispatch_task()` 已形成统一拦截链 |
| REQ-OP-04 | 1200 车辆运维 | 车辆登记、状态、故障上报、工单 | REDLINE-03 | 已实现达标 | `vehicle_management`、`ensure_vehicle_registry()`、工单创建链路已可用 |
| REQ-OP-05 | 停车点实时车辆数直接展示 | 地图 62 个标记均直接显示实时车辆数，10 秒刷新同步 | REDLINE-01/05/06 | 已实现达标 | `/operation/heatmap/` 与 `/operation/` 地图标记直接显示 `count`；自动化校验 `marker_count_field_present=true` |
| REQ-OP-06 | 供需表格/柱状图并列视图 | 热力图页面支持表格/柱状图切换、排序、点行联动定位地图 | REDLINE-05/06 | 已实现达标 | 热力图页面新增 `dispatchTableView` / `dispatchChartView`，表头排序与 `focusStation()` 联动已补齐 |
| REQ-OP-07 | 车辆状态与位置查询 | 1200 车辆分页、筛选、详情轨迹、故障转待维修工单 | REDLINE-03/06 | 已实现达标 | 新增 `VehicleLocationHistory`、车辆详情页、站点/状态筛选、故障联动工单与关联车辆 ID |
| REQ-SYS-01 | RBAC 权限控制 | 管理员/运维调度员/数据分析员访问边界明确，越权返回 403 | REDLINE-06 | 已实现达标 | `system_support.permissions` 已生效；自动化校验通过 |
| REQ-SYS-02 | 导出能力 | 预测报告、调度记录支持 CSV/Excel 导出 | REDLINE-06 | 已实现达标 | 新增 `export_prediction_report`、`task_export`、站点历史双格式导出 |
| REQ-SYS-03 | 10 秒刷新与 SQLite 锁稳定 | 轮询不引发锁库，串行锁保留 | REDLINE-06 | 已实现达标 | `runtime_service` 快照锁保留；20 并发探针无异常 |

## 4. 非功能性需求校验
| 编号 | 需求名称 | 验收标准 | 当前状态 | 证据/说明 |
| --- | --- | --- | --- | --- |
| REQ-NFR-01 | 预测 API 响应时间 | 常规访问 `<1000ms` | 已实现达标 | 自动化校验 `prediction_api.latency_ms=176.42` |
| REQ-NFR-02 | 运行时快照生成 | 常规刷新 `<500ms` | 已实现达标 | 自动化校验 `runtime_snapshot.latency_ms=59.48` |
| REQ-NFR-03 | 地图/页面可用性 | 根路径跳转正确，页面访问稳定 | 已实现达标 | `/ -> /system/dashboard/`；`/operation/heatmap/`、`/operation/vehicles/`、详情页均返回 200 |
| REQ-NFR-04 | 并发稳定性 | `>=20` 并发无锁库、无总量漂移 | 已实现达标 | 自动化校验 `workers=20, errors=[]` |
| REQ-NFR-05 | 异常友好性 | 未授权返回 403，页面显示友好提示，不暴露堆栈 | 已实现达标 | 403 页面模板与 JSON 403 响应已生效 |
| REQ-NFR-06 | 车辆筛选响应时间 | 车辆状态/站点筛选 `<2s` | 已实现达标 | 自动化校验 `vehicle_filter_latency_ms < 2000` |
| REQ-NFR-07 | 连续 72 小时稳定运行 | 无崩溃、无数据库锁死、无总量失衡 | 已补全待终验 | 已新增 `run_srs_soak_test.py`，需最终执行完整 72 小时实跑 |

## 5. 不纳入当前主线验收的事项
| 项 | 说明 |
| --- | --- |
| `django_project/manage.py` | 视为遗留兼容入口，不作为本次 SRS 主线验收对象 |
| 原生产模型文件覆盖 | 严格禁止；仅允许生成并行 `*_srs` 资产 |
| 已归档历史文档重写 | 不纳入本次变更范围，仅新增本轮验收文档 |

## 6. 当前结论
- REQ-OP-05、REQ-OP-06、REQ-OP-07 已按 SRS V3.2 以非破坏性增量方式补全。
- 当前主线代码、页面、权限、导出和自动化验收脚本已经同步覆盖这三项新增运维需求。
- 仍需最终执行类验收的事项只有两项：
  - 运行 `build_lstm_system_srs.py` 生成并行 SRS 模型资产，并确认 `T+1` 非零净流量准确率是否达到 `>=85%`
  - 执行 `run_srs_soak_test.py --duration-hours 72` 完成连续 72 小时稳定性验收
