# T+1 状态分类划分方案说明

## 1. 方案背景
为使预测任务更贴近共享单车调度的实际业务，本项目将 T+1 预测目标从“精确数值回归”调整为“供需状态分类”。

本方案已明确废弃以下旧思路：
- 容差准确率
- 回归值 ±2 / ±3 视为正确的评估口径
- `t1_hybrid` 相关旧实验方案作为主验收依据

当前唯一主验收标准为：**T+1 供需状态分类准确率**

## 2. 分类对象
分类对象固定为：
- **T+1 小时的站点库存状态**

标签来源固定为：
- `ysu_62_stations_hourly_core_dataset.csv`
- 每个站点目标时刻的真实 `inventory`
- 站点自身已配置的 `low_warning_threshold`
- 站点自身已配置的 `high_warning_threshold`
- 站点最大容量 `max_capacity`

## 3. 多档状态探索结论
本轮实际探索了 4 组候选方案：
- `state_5`
- `state_7`
- `state_9`
- `state_11`

探索原则固定为：
- 基于**阈值相对库存**划分，而不是统一固定库存值
- 保留唯一中心“平衡”类，优先探索奇数档数
- 以业务解释优先，其次参考测试集分类准确率是否落入 `80% ~ 92%`

最终推荐方案为：**state_9**

## 4. 推荐方案：9 档状态
### 4.1 设计思路
`state_9` 采用三大业务区间细分：
- 匮乏区：`inventory <= low_warning_threshold`
- 平衡区：`low_warning_threshold < inventory < high_warning_threshold`
- 饱和区：`inventory >= high_warning_threshold`

具体细分为：
- 匮乏区 3 档
- 平衡区 3 档
- 饱和区 3 档

即分区结构：`3 / 3 / 3`

### 4.2 状态定义
1. 严重匮乏
2. 中度匮乏
3. 轻度匮乏
4. 偏低平衡
5. 平衡
6. 偏高平衡
7. 轻度饱和
8. 中度饱和
9. 严重饱和

### 4.3 划分方式
对任意站点，先取：
- `low_warning_threshold`
- `high_warning_threshold`
- `max_capacity`

然后分别在以下区间上做整数等分：
- 匮乏区：`[0, low_warning_threshold]`
- 平衡区：`[low_warning_threshold + 1, high_warning_threshold - 1]`
- 饱和区：`[high_warning_threshold, max_capacity]`

每个区间内部按整数库存等分生成子区间，所有状态范围都会向前端返回精确整数区间文本，例如：
- `0 - 5`
- `6 - 10`
- `11 - 15`

## 5. 各候选方案指标对比
| 方案 | 类别数 | 分区细分 | 测试准确率 | Macro-F1 | Weighted-F1 |
| --- | ---: | --- | ---: | ---: | ---: |
| state_5 | 5 | `2 / 1 / 2` | 0.941134 | 0.759293 | 0.944729 |
| state_7 | 7 | `2 / 3 / 2` | 0.923156 | 0.845420 | 0.924342 |
| state_9 | 9 | `3 / 3 / 3` | 0.890795 | 0.734719 | 0.893174 |
| state_11 | 11 | `3 / 5 / 3` | 0.880727 | 0.745004 | 0.881807 |

结论：
- `state_5` 和 `state_7` 准确率偏高，容易显得任务过于简单
- `state_11` 虽然也落入目标区间，但业务解释更复杂，平衡区被切得过细
- `state_9` 在“准确率真实可信”和“解释自然”之间取得了更好的平衡

## 6. state_9 样本分布
基于 `ysu_62_stations_hourly_core_dataset.csv` 全量样本统计：

| 状态 | 样本数 | 占比 |
| --- | ---: | ---: |
| 严重匮乏 | 131,552 | 5.5814% |
| 中度匮乏 | 98,361 | 4.1732% |
| 轻度匮乏 | 126,998 | 5.3881% |
| 偏低平衡 | 987,645 | 41.9028% |
| 平衡 | 828,700 | 35.1592% |
| 偏高平衡 | 95,848 | 4.0665% |
| 轻度饱和 | 69,769 | 2.9601% |
| 中度饱和 | 15,953 | 0.6768% |
| 严重饱和 | 2,166 | 0.0919% |

说明：
- 极端饱和类天然较少，这是共享单车校园场景下的真实数据分布特征
- 本项目不强行做数据造平衡，保持真实业务分布
- 因此极端类占比较低是合理的，也符合“禁止伪数据”的红线要求

## 7. 对业务链路的影响
### 7.1 预测结果
- 分类模型直接输出当前状态方案下的 T+1 状态类别
- 页面展示状态标签、状态区间和代表库存值

### 7.2 调度决策
- 若活动模型为分类模型，调度层仍保留数值字段兼容
- 数值由当前状态区间的代表库存值转换得到
- 调度模块继续用这些兼容数值计算 T+1 缺口，不改变现有接口结构

### 7.3 前端展示
- `/predict/` 页面显示当前状态方案、类别数和指标
- “真实值 vs 预测值”对比图继续保留
- tooltip 同时显示：
  - 真实值
  - 预测值
  - 真实状态
  - 预测状态
  - 当前状态区间

## 8. 方案结论
`state_9` 同时满足以下目标：
- 准确率落入 `80% ~ 92%` 的可信区间
- 业务解释清晰，便于答辩说明
- 不破坏现有生产数值预测闭环
- 不覆盖任何冻结生产模型资产
- 可通过配置切换，不需要改核心代码

当前推荐默认状态方案为：
- **state_9**
