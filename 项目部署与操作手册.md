# 项目部署与操作手册

## 1. 项目说明
本系统为“基于深度学习的城市共享单车调度需求预测与运维管理平台”，主线工程固定为 `bike_dispatch_platform`。

当前预测体系已经调整为双轨并行：
- `production`：冻结生产 48→48 数值预测模型
- `t1_state_classifier`：并行 T+1 多档供需状态分类模型

说明：
- 默认活动模型仍为 `production`
- 容差准确率方案已废弃，不再作为任何评估或验收依据
- 当前唯一业务验收指标为 **T+1 状态分类准确率**
- 当前默认状态方案为 **state_9**

## 2. 环境要求
- Python 3.9+
- Django 4.2+
- TensorFlow 2.10.0
- SQLite
- Windows + CUDA 11.2 + cuDNN 8.1（如需 GPU 训练）

## 3. 安装依赖
```bash
pip install -r requirements.txt
```

## 4. 主线启动方式
```bash
python manage.py migrate --settings=bike_dispatch_platform.bike_dispatch_platform.settings
python manage.py runserver --settings=bike_dispatch_platform.bike_dispatch_platform.settings
```

访问地址：
- 根路径：`http://127.0.0.1:8000/`
- 登录页：`http://127.0.0.1:8000/system/login/`
- 系统首页：`http://127.0.0.1:8000/system/dashboard/`

## 5. 角色与权限
### 5.1 系统管理员
- 可访问：总览、预测、调度、站点运维、车辆运维、数据管理、系统设置、备份、日志、导出
- 可执行：模型切换、状态方案切换、手动训练、调度管理、运维工单、系统配置修改

### 5.2 运维调度员
- 可访问：总览、预测、调度热力图、站点运维、车辆运维
- 可执行：手动调度、调度任务处理、工单创建、调度记录导出
- 不可访问：系统设置、备份、日志、数据管理

### 5.3 数据分析员
- 可访问：总览、预测、数据管理
- 可执行：查看预测结果、导出预测报告、只读查看数据
- 不可访问：调度写操作、系统设置、备份、日志

## 6. 核心页面说明
### 6.1 系统总览
- 路径：`/system/dashboard/`
- 功能：
  - 4 个 KPI 卡片
  - 62 站点地图
  - 10 秒自动刷新
  - 实时趋势图与调度建议

### 6.2 需求预测
- 路径：`/predict/`
- 功能：
  - 未来 48 小时数值趋势展示
  - T+1 多档状态标签、状态区间与代表值展示
  - 最近 24/48 小时“真实值 vs 预测值”回溯对比图
  - 当前活动模型、当前状态方案、分类指标展示
  - 预测报告导出

### 6.3 调度热力图与任务
- 路径：`/operation/heatmap/`、`/operation/tasks/`
- 功能：
  - 62 站点热力图
  - 实时库存数字标记
  - 表格 / 柱状图视图
  - 自动调度与手动调度

### 6.4 站点运维与车辆运维
- 路径：
  - `/operation/stations/`
  - `/operation/vehicles/`
- 功能：
  - 站点筛选、导出、编辑非核心运维字段
  - 1200 辆车辆状态、位置、轨迹、故障上报、工单联动

### 6.5 系统设置、备份与日志
- 路径：
  - `/system/settings/`
  - `/system/backups/`
  - `/system/logs/`
- 功能：
  - 仪表盘刷新频率、阈值、活动模型配置
  - 状态方案配置
  - 数据库备份
  - 系统日志查看

## 7. 预测模型资产
### 7.1 生产模型
- `bike_dispatch_platform/demand_prediction/model_assets/bike_sharing_lstm_model.keras`
- `bike_dispatch_platform/demand_prediction/model_assets/scaler.pkl`

说明：
- 保持冻结，不覆盖、不删除
- 继续承担 48 小时数值预测主链路

### 7.2 并行状态分类模型
已生成以下并行资产：
- `bike_sharing_lstm_state_classifier_5class.keras`
- `bike_sharing_lstm_state_classifier_7class.keras`
- `bike_sharing_lstm_state_classifier_9class.keras`
- `bike_sharing_lstm_state_classifier_11class.keras`

对应配套文件：
- `state_classifier_bundle_5class.pkl`
- `state_classifier_bundle_7class.pkl`
- `state_classifier_bundle_9class.pkl`
- `state_classifier_bundle_11class.pkl`
- `model_metrics_state_classifier_5class.json`
- `model_metrics_state_classifier_7class.json`
- `model_metrics_state_classifier_9class.json`
- `model_metrics_state_classifier_11class.json`
- `confusion_matrix_state_classifier_5class.json`
- `confusion_matrix_state_classifier_7class.json`
- `confusion_matrix_state_classifier_9class.json`
- `confusion_matrix_state_classifier_11class.json`

说明：
- 仅用于 T+1 多档供需状态分类
- 不覆盖生产模型

### 7.3 模型与状态方案配置
配置文件：
- `bike_dispatch_platform/demand_prediction/model_assets/model_selection.json`

关键字段：
- `active_model_alias`
- `active_state_scheme_key`
- `recommended_state_scheme_key`

当前推荐值：
- `active_model_alias = production`
- `active_state_scheme_key = state_9`
- `recommended_state_scheme_key = state_9`

## 8. 模型训练与评估
### 8.1 训练当前状态方案分类模型
```bash
python build_t1_state_classifier.py
```

按指定方案训练：
```bash
python build_t1_state_classifier.py --scheme state_9
```

### 8.2 探索所有候选状态方案
```bash
python explore_state_schemes.py
```

该脚本会：
- 训练 `state_5 / state_7 / state_9 / state_11`
- 生成多方案资产文件
- 输出 `状态划分探索报告.md`
- 更新推荐状态方案到 `model_selection.json`

### 8.3 评估分类准确率
评估生产模型映射后的状态分类表现：
```bash
python evaluate_t1_accuracy.py --alias production --scheme state_9
```

评估并行状态分类模型：
```bash
python evaluate_t1_accuracy.py --alias t1_state_classifier --scheme state_9
```

说明：
- `evaluate_t1_accuracy.py` 已不再计算任何容差准确率
- 当前唯一主指标为：
  - `classification_accuracy`
  - `macro_f1`
  - `weighted_f1`
  - `confusion_matrix`

## 9. 预测对比图接口
### 9.1 接口地址
```text
GET /predict/api/compare/
```

### 9.2 请求参数
- `station_id`
- `hours=24|48`

### 9.3 返回内容
- `timestamps`
- `actual_values`
- `predicted_values`
- `actual_states`
- `predicted_states`
- `state_ranges`
- `model_alias`
- `state_scheme_key`

## 10. 切换方式
### 10.1 页面切换
- 进入 `/system/settings/`
- 在“活动预测模型”中选择 `production` 或 `t1_state_classifier`
- 在“当前状态方案”中选择 `state_5 / state_7 / state_9 / state_11`
- 保存设置

### 10.2 直接修改配置文件
```json
{
  "active_model_alias": "production",
  "active_state_scheme_key": "state_9",
  "recommended_state_scheme_key": "state_9"
}
```

### 10.3 回退方式
- 将 `active_model_alias` 改回 `production`
- 如需回退状态方案，将 `active_state_scheme_key` 改回目标方案（如 `state_5`）

## 11. 自动化验收与长稳测试
### 11.1 SRS 合规校验
```bash
python verify_srs_compliance.py
```

### 11.2 72 小时长稳测试
```bash
python run_srs_soak_test.py --duration-hours 72 --interval-seconds 300
```

日志输出：
- `soak_test_YYYYMMDD_HHMMSS.jsonl`

## 12. 常见问题
### 12.1 为什么现在不再显示容差准确率？
- 因为容差机制已被正式废弃
- 当前唯一验收标准是 T+1 状态分类准确率

### 12.2 为什么最终推荐 state_9？
- 因为 `state_9` 的测试准确率约为 `89.08%`
- 它既落在 `80% ~ 92%` 的目标区间内，又保留了较强业务解释性

### 12.3 为什么需求预测页既有未来 48 小时图，又有历史回溯对比图？
- 未来 48 小时图用于展示系统未来趋势
- 历史回溯对比图用于答辩和验收时直观展示“真实值 vs 预测值”

### 12.4 如果页面返回 403 怎么办？
- 系统已启用 RBAC
- 请使用具备对应角色权限的账号访问相应模块
