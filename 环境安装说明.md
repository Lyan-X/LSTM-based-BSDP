# 环境安装说明 - 解决 "No module named 'django'"

## 错误原因

`ModuleNotFoundError: No module named 'django'` 表示当前 Python 环境中**没有安装 Django**，常见原因：

1. **未激活虚拟环境**（若项目使用 venv）
2. **未安装依赖**（未执行过 `pip install -r requirements.txt`）

---

## 解决步骤

### 方式一：直接安装依赖（推荐先试）

在项目根目录 `BSDP` 下打开终端，执行：

```powershell
# 1. 进入项目目录
cd "E:\develop\BSDP-Bike Sharing Demand Prediction Based on LSTM Model\BSDP"

# 2. 安装所有依赖（含 Django）
pip install -r requirements.txt
```

若出现网络超时或下载慢，可使用国内镜像：

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

安装完成后再执行：

```powershell
cd bike_dispatch_platform
python manage.py makemigrations
python manage.py migrate
```

---

### 方式二：使用虚拟环境（推荐长期使用）

```powershell
# 1. 进入项目根目录
cd "E:\develop\BSDP-Bike Sharing Demand Prediction Based on LSTM Model\BSDP"

# 2. 创建虚拟环境
python -m venv venv

# 3. 激活虚拟环境（Windows PowerShell）
.\venv\Scripts\Activate.ps1

# 若提示无法执行脚本，先运行：
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 4. 安装依赖
pip install -r requirements.txt

# 5. 进入 Django 项目目录并执行迁移
cd bike_dispatch_platform
python manage.py makemigrations
python manage.py migrate
```

**之后每次开发前**，先激活虚拟环境再运行项目：

```powershell
cd "E:\develop\BSDP-Bike Sharing Demand Prediction Based on LSTM Model\BSDP"
.\venv\Scripts\Activate.ps1
cd bike_dispatch_platform
python manage.py runserver
```

---

## 若 pip 安装报错

### 报错：`Could not find a version that satisfies the requirement django==4.2.10`

- 先确认网络正常，再试国内镜像：
  ```powershell
  pip install django==4.2.10 -i https://pypi.tuna.tsinghua.edu.cn/simple
  ```
- 若仍失败，可放宽版本再装 Django：
  ```powershell
  pip install django
  ```
  然后再装其余依赖：`pip install -r requirements.txt`（或逐个安装）。

### 报错：`No module named 'django'` 仍存在

- 确认当前终端里用的 Python 是否和安装依赖时一致：
  ```powershell
  where python
  pip show django
  ```
- 若使用了虚拟环境，务必先执行 `.\venv\Scripts\Activate.ps1` 再运行 `python manage.py ...`。

---

## 快速检查是否安装成功

```powershell
pip show django
```

若能看到 Django 的版本信息，说明已安装成功，再执行：

```powershell
cd bike_dispatch_platform
python manage.py makemigrations
```

即可正常生成迁移。
