# LSTM模型优化核心指标对比报告

## 1. 核心指标对比

| 模型版本 | 测试集 MAE | 测试集 RMSE | 测试集 R² |
| --- | ---: | ---: | ---: |
| 原版生产 LSTM | 0.3675804138 | 0.8602672815 | -0.0095100102 |
| 优化版 LSTM | 0.3151459992 | 0.8216607571 | 0.0790653592 |
| BP 基准模型 | 0.3214291632 | 0.8382275701 | 0.0415541883 |

## 2. 指标变化

相对于原版生产 LSTM：

- 优化版 LSTM 的 MAE 下降：`0.0524344146`
- 优化版 LSTM 的 RMSE 下降：`0.0386065245`
- 优化版 LSTM 的 R² 提升：`0.0885753694`

相对于 BP 基准模型：

- 优化版 LSTM 的 MAE 更低
- 优化版 LSTM 的 RMSE 更低
- 优化版 LSTM 的 R² 更高

结论：在本次严格保持生产链路兼容的独立验证中，优化版 LSTM 是三者中综合效果最好的模型。

## 3. 测试集拟合效果说明

- 原版生产 LSTM 的测试集 `R²` 为负值，说明其对小时级净流量波动的解释力非常有限，预测容易退化为接近零的平滑输出。
- 优化版 LSTM 将 `R²` 提升到正值，同时 `MAE/RMSE` 持续下降，说明模型不仅误差下降，而且开始具备一定的时序解释能力。
- BP 基准模型同样优于原版生产模型，但整体仍弱于优化版 LSTM，说明在本项目的 48 小时时序预测任务中，LSTM 仍然更符合研究定位与效果需求。
- 从测试样例看，优化版 LSTM 对低幅波动和方向变化的感知有所增强，但对突发高峰的峰值幅度仍然偏保守，峰值拟合尚未达到理想状态。

## 4. R² 提升原因分析

- 优化版模型保留了 LSTM 作为核心架构，符合本科毕设开题报告与任务书的研究定位。
- 在单输入单输出的兼容前提下，加入了多头注意力与残差结构，增强了模型对 48 小时序列内部关键时间片的关注能力。
- 训练输入中显式纳入了站点标识、经纬度、容量、初始库存、阈值等静态站点属性，使模型能够区分不同燕大站点的一对一映射序列，不再像原版那样容易出现“多站点输出过度相似”的退化现象。
- 保留了时间顺序切分与 `shuffle=False` 的合规逻辑，避免数据穿越，使指标具备学术可解释性。

## 5. 答辩可复用学术解读

- 本项目的核心任务是“过去 48 小时输入，预测未来 48 小时净流量”，属于典型多步时序预测问题。
- 在该任务中，原版生产模型虽然能支撑工程闭环，但解释力不足，尤其对共享单车早晚高峰等突发流量波动的拟合能力较弱。
- 优化版模型在不改变生产系统任何业务代码、不破坏现有 62 站点映射与 1200 辆车辆守恒规则的前提下，实现了误差指标和解释指标的同步改善，证明“LSTM + 注意力增强 + 兼容性静态特征补充”是有效方向。
- 需要如实说明的是，本次独立优化验证尚未达到目标阈值 `R² ≥ 0.3`，更没有达到冲击 `0.6+` 的理想状态，因此当前成果更适合作为“显著优于原版、但仍需继续迭代”的论文优化结论，而不是夸大为最终最优模型。

## 6. 最终结论

- 是否满足“以 LSTM 为核心模型”的要求：满足
- 是否优于原版生产模型：满足
- 是否优于 BP 基准模型：满足
- 是否保持与现有业务链路输入输出格式完全一致：满足
- 是否达到 `R² ≥ 0.3`：未达到

当前最终效果等级：

`可作为独立优化验证版本与答辩展示的增强模型，但暂不建议未经进一步验证直接替换生产模型。`
