# 数据导入指南

## 一、测试数据说明

### 1. 骑行数据
- **文件路径**：`test_data/ride_data_test.csv`
- **数据格式**：CSV格式
- **包含字段**：
  - data_source：数据来源
  - start_point：骑行起点（燕山大学东校区、西校区等）
  - end_point：骑行终点
  - ride_datetime：骑行时间
  - duration：骑行时长（分钟）
  - distance：骑行距离（公里）
  - status：数据状态（cleaned）

### 2. 天气数据
- **文件路径**：`test_data/weather_data_test.csv`
- **数据格式**：CSV格式
- **包含字段**：
  - area：区域（与骑行数据的start_point对应）
  - date：日期
  - temperature：温度(℃)
  - humidity：湿度(%)
  - wind_speed：风速(m/s)
  - rainfall：降雨量(mm)
  - weather_type：天气类型（sunny/cloudy/rain）

## 二、数据导入步骤

### 1. 登录系统
- **访问地址**：http://localhost:8000/system/login/
- **用户名**：Admin（或您创建的超级用户）
- **密码**：您设置的密码

### 2. 导入骑行数据
1. 登录后，点击导航栏中的「数据处理」
2. 在数据上传页面：
   - 选择「骑行数据上传」
   - 数据来源：填写「测试数据」
   - 选择文件：选择 `test_data/ride_data_test.csv`
   - 点击「上传并清洗」
   - 等待上传完成，查看预览数据

### 3. 导入天气数据
1. 在数据处理页面，选择「天气数据上传」
2. 数据来源：填写「测试数据」
3. 选择文件：选择 `test_data/weather_data_test.csv`
4. 点击「上传并入库」
5. 等待上传完成

## 三、数据验证

### 1. 查看骑行数据
- 上传完成后，系统会自动跳转到数据列表页面
- 您可以看到所有导入的骑行数据
- 每条数据都会显示骑行时间、起点、终点等信息

### 2. 查看天气数据
- 在数据处理页面，选择「天气数据列表」
- 您可以看到所有导入的天气数据
- 每条数据都会显示区域、日期、温度等信息

## 四、注意事项

1. **数据格式**：请确保CSV文件格式正确，字段名称与示例数据一致
2. **日期格式**：日期时间格式应为 `YYYY-MM-DD HH:MM:SS`
3. **数据量**：单次上传建议不超过1000条数据
4. **数据清洗**：系统会自动对数据进行基本清洗和验证

## 五、常见问题

### 1. 上传失败
- 检查文件格式是否正确
- 检查字段名称是否与要求一致
- 检查文件编码是否为UTF-8

### 2. 数据不显示
- 检查数据状态是否为 "cleaned"
- 检查上传用户是否正确
- 检查数据库连接是否正常

### 3. 天气数据关联失败
- 确保天气数据的 `area` 字段与骑行数据的 `start_point` 字段匹配
- 确保日期范围覆盖骑行数据的日期

---

**提示**：导入测试数据后，您可以在「需求预测」模块中进行预测测试，在「运维管理」模块中查看供需热力图。