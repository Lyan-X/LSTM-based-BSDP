# 虚拟环境（体积大，无需提交）
bsdp_env/
# 模型文件（若超过100MB，GitHub无法直接提交，建议注释说明获取方式）
models/bike_lstm_model.h5
# Python缓存文件
__pycache__/
*.pyc
*.pyd
# IDE配置文件（VSCode/PyCharm）
.idea/
.vscode/
# 系统临时文件
.DS_Store
Thumbs.db
# 补充过滤缓存/日志文件
*.log
# 过滤原始数据集（仅保留预处理脚本，避免大文件）
data/raw/
# 可选：过滤可视化结果（若图片体积大，也可保留小体积的prediction_result.png）
# results/
