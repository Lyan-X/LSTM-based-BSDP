# 优化模型生产替换极简指南

## 第 1 步：在项目根目录手工导出兼容版优化模型

执行：

```powershell
python build_lstm_system_optimized.py --save-model
```

会在项目根目录生成：

- `bike_sharing_lstm_model_optimized.keras`
- `optimized_feature_scaler.pkl`

这两个文件保持单输入单输出格式，可对接现有生产调用链路。

## 第 2 步：备份后再替换，异常时立即回滚

先手工备份生产目录 `bike_dispatch_platform/demand_prediction/model_assets/` 中的：

- `bike_sharing_lstm_model.keras`
- `scaler.pkl`

然后把根目录导出的两个文件复制到该目录，并重命名为：

- `bike_sharing_lstm_model.keras`
- `scaler.pkl`

如果替换后效果不理想或出现异常，直接把刚才备份的原始生产文件复制回去即可完成回滚，无需修改任何业务代码、接口或前端页面。
