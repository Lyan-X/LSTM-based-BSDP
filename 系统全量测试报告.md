# 系统全量测试报告

## 1. 测试对象
- 项目名称：基于深度学习的城市共享单车调度需求预测与运维管理平台
- 验收主线：`bike_dispatch_platform`
- 验收时间：2026-04-17
- 验收脚本：
  - `verify_srs_compliance.py`
  - `run_srs_soak_test.py`

## 2. 测试范围
- 红线合规性测试
- 功能测试
- 权限与接口测试
- 性能与稳定性测试
- 并发与锁库测试
- 教师建议新增运维功能测试（REQ-OP-05 / REQ-OP-06 / REQ-OP-07）

## 3. 自动化验收结果摘要
基于最新 `python verify_srs_compliance.py` 的结果：

| 指标 | 结果 |
| --- | --- |
| 根路径跳转 | `302 -> /system/dashboard/`，通过 |
| 活跃站点数 | `62`，通过 |
| 全局车辆总数 | `1200`，通过 |
| 预测 API 常规响应时间 | `176.42ms`，通过 |
| 运行时快照常规响应时间 | `59.48ms`，通过 |
| 热力图别名路由 `/operation/heatmap/` | `200`，通过 |
| 热力图数据行数 | `62`，通过 |
| 地图标记直接显示车辆数 | 通过 |
| 表格/柱状图并列视图 | 通过 |
| 表格排序与地图联动脚本 | 通过 |
| 车辆总数 | `1200`，通过 |
| 车辆列表页 | `200`，通过 |
| 车辆详情页 | `200`，通过 |
| 车辆筛选响应时间 | `< 2s`，通过 |
| 故障标记自动生成工单 | 通过 |
| 20 并发稳定性 | `errors=[]`，通过 |
| RBAC 权限矩阵 | 全部通过 |

## 4. 功能测试结果
| 模块 | 测试点 | 结果 | 说明 |
| --- | --- | --- | --- |
| 数据模块 | 数据只读接入、统计接口、导出 | 通过 | 数据管理模块仅允许管理员与数据分析员访问 |
| 预测模块 | `/predict/api/48h/`、`/predict/api/station/<id>/`、预测页、结果页、导出 | 通过 | 已补齐 T+1 字段与预测报告导出 |
| 调度模块 | 自动调度、手动调度、调度记录导出 | 通过 | 调度口径已切换为 T+1，手动调度保留三重校验 |
| 运维模块 | 站点运维、车辆运维、故障上报、工单创建 | 通过 | 运维调度员与管理员可操作 |
| 系统模块 | 登录、总览、设置、备份、日志 | 通过 | 管理员拥有完整权限 |
| 403 处理 | 未授权访问敏感页面/API | 通过 | HTML 页面与 JSON 接口均返回 403 |

## 5. 新增教师建议功能专项测试
### 5.1 REQ-OP-05：停车点实时车辆数直接展示
- 测试方法：
  - 访问 `/operation/heatmap/`
  - 调用 `/operation/api/parking-data/`
  - 校验 62 个站点数据均包含 `count`
  - 校验热力图模板保留数字 Marker 直接展示逻辑
- 测试结果：
  - 热力图页面返回 `200`
  - 实时数据接口返回 `62` 条站点记录
  - 每条记录均携带实时车辆数字段
  - 地图标记仍采用原有红黄绿逻辑和原始点位
- 结论：通过

### 5.2 REQ-OP-06：供需表格/柱状图并列视图
- 测试方法：
  - 打开热力图页面，检查表格视图和柱状图视图切换按钮
  - 检查表头排序脚本是否存在
  - 检查表格行点击是否调用地图定位联动函数
  - 检查数据是否随 10 秒轮询刷新
- 测试结果：
  - 页面存在 `dispatchTableView` 与 `dispatchChartView`
  - 排序表头 `.sortable` 已接入排序逻辑
  - `focusStation()` 已接入地图平移与高亮
  - 表格与图表共享同一批实时数据，随轮询同步更新
- 结论：通过

### 5.3 REQ-OP-07：车辆状态与位置查询
- 测试方法：
  - 访问 `/operation/vehicles/`
  - 校验分页、站点筛选、状态筛选
  - 打开 `/operation/vehicles/<vehicle_id>/`
  - 检查最近 10 次轨迹记录
  - 提交故障标记操作，检查工单是否自动生成且关联车辆 ID
- 测试结果：
  - 车辆总数为 `1200`
  - 列表页返回 `200`
  - 详情页返回 `200`
  - 车辆轨迹使用 `VehicleLocationHistory` 存储，详情页展示最近 10 条
  - 故障标记后自动生成故障记录与维修工单，`ScheduleTask.related_vehicle` 已关联车辆 ID
  - 筛选响应时间满足 `< 2s`
- 结论：通过

## 6. 权限测试结果
### 6.1 系统管理员
- 可访问：总览、预测、调度热力图、站点运维、车辆运维、数据管理、系统设置、备份、日志
- 测试结论：全部返回 `200`

### 6.2 运维调度员
- 可访问：总览、预测、调度热力图、站点运维、车辆运维
- 禁止访问：数据管理、系统设置、备份、日志
- 测试结论：授权页面返回 `200`，未授权页面统一返回 `403`

### 6.3 数据分析员
- 可访问：总览、预测、数据管理
- 禁止访问：调度热力图、站点运维、车辆运维、系统设置、备份、日志
- 测试结论：授权页面返回 `200`，未授权页面统一返回 `403`

## 7. 红线复检结果
| 红线项 | 结果 | 说明 |
| --- | --- | --- |
| 62 站点固定 | 通过 | 活跃站点数恒定为 62 |
| 真实映射数据 | 通过 | 预测与运行时链路仍使用核心映射数据集 |
| 1200 车辆守恒 | 通过 | 快照、调度、车辆登记均以 1200 为硬约束 |
| 48→48 与时序切分 | 通过 | 训练与推理逻辑未改变基本时序口径 |
| 既有 UI/地图风格不变 | 通过 | 仅增强数据视图与车辆查询，不重做地图视觉 |
| 10 秒刷新 / 串行锁 | 通过 | 快照锁保留，20 并发未出现锁库 |
| 技术中立性 | 通过 | 新增并行 SRS 训练脚本，不覆盖生产模型 |

## 8. 性能与稳定性说明
- 预测 API 响应时间：常规访问满足 `<1000ms`
- 运行时快照：常规访问满足 `<500ms`
- 车辆筛选响应时间：满足 `<2s`
- 并发测试：20 并发探针未出现 `database is locked`
- 72 小时长稳测试：脚本已准备完毕，需在最终验收阶段执行完整实跑

## 9. 已知待执行事项
以下两项属于“最终执行类验收”，不是代码缺陷：
- 运行 `python build_lstm_system_srs.py` 生成并行 SRS 模型资产，并核验 `T+1` 非零净流量准确率是否达到 `>=85%`
- 运行 `python run_srs_soak_test.py --duration-hours 72 --interval-seconds 300` 完成连续 72 小时稳定性验收

## 10. 结论
- REQ-OP-05、REQ-OP-06、REQ-OP-07 已完成，并已纳入自动化验收脚本。
- 当前主线系统在不破坏既有生产闭环的前提下，已经补齐教师建议新增的三项运维功能。
- 剩余需要最终落地的内容仍为并行 SRS 模型资产训练验收与 72 小时连续实跑验收，两项所需脚本均已交付。 
