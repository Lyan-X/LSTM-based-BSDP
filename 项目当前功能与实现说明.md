# 基于深度学习的城市共享单车调度需求预测与运维管理平台 当前功能与实现说明

## 1. 项目当前整体能实现什么

当前项目已经具备一套从真实数据映射、模型预测、实时展示、供需缺口分析、调度建议生成，到运维管理与系统支撑的完整闭环能力，核心可以概括为：

1. 基于华盛顿真实骑行数据，构建燕山大学 62 个停车点的小时级核心数据集。
2. 基于已训练完成的 LSTM 模型，对 62 个站点未来 48 小时净流量进行批量预测。
3. 将预测结果转换为未来库存轨迹，并严格维持全局 1200 辆车辆守恒。
4. 以 10 秒为刷新粒度生成实时站点状态，用于仪表盘、地图、预警和调度展示。
5. 根据预测缺口与实时库存生成供需分析结果，并在满足条件时给出调度建议。
6. 支持站点运维、人工调度、车辆运维、故障上报、工单流转、系统设置、备份与日志查看。

## 2. 核心数据基础是怎么实现的

### 2.1 62 站点主数据

项目使用根目录下的 [ysu_62_station_master_data.csv](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/ysu_62_station_master_data.csv) 和 [ysu_62_station_mapping.csv](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/ysu_62_station_mapping.csv) 作为燕山大学 62 个停车点的主数据与映射基准。

这些数据包含：

- 站点编号 `ysu_id`
- 站点名称
- 经纬度
- 最大容量
- 初始库存
- 预警阈值
- 对应华盛顿真实站点的映射关系

系统运行时会通过站点同步服务将这套主数据同步到 Django 的 `ParkingSpot` 模型中，所有业务都以这套主数据为准。

### 2.2 小时级核心数据集

项目根目录下的 [ysu_62_stations_hourly_core_dataset.csv](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/ysu_62_stations_hourly_core_dataset.csv) 是当前所有训练、预测、验证和可视化的统一数据基础。

这份数据集的特点是：

- 数据来源是华盛顿 Kaggle Capital Bikeshare 真实骑行订单
- 通过 1:1 映射逻辑迁移到燕山大学 62 个停车点
- 粒度是“站点 + 小时”
- 包含 `inflow`、`outflow`、`net_flow`、`inventory` 等关键字段
- 全局车辆总数严格受 1200 守恒规则约束

## 3. 各模块目前能实现的功能，以及怎么实现的

### 3.1 路由与系统入口

当前系统访问根路径 `/` 时，会自动跳转到 `/system/dashboard/`，也就是系统首页。

实现方式：

- 主路由在 [bike_dispatch_platform/bike_dispatch_platform/urls.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/bike_dispatch_platform/urls.py) 中配置
- 使用 Django `RedirectView` 将根路径重定向到仪表盘页面
- 同时挂载了 `data/`、`predict/`、`operation/`、`system/` 四个业务模块的路由

### 3.2 登录、权限与首页

系统支持登录、登出、角色区分和首页访问控制。

实现方式：

- 登录与登出逻辑在 [bike_dispatch_platform/system_support/views.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/system_support/views.py)
- 登录后默认进入首页仪表盘
- 管理员权限用于系统设置、备份、日志等高权限页面
- 每次登录、登出、设置修改、备份等操作都会写入系统日志

### 3.3 数据概览与导出

系统目前可以展示真实骑行数据概览，并支持导出数据。

实现方式：

- 数据模块入口在 [bike_dispatch_platform/data_process/views.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/data_process/views.py)
- 页面直接只读读取根目录下的 `ysu_bike_data.csv`
- 可展示总记录数、时间范围、字段列表、样例停车点和预览数据
- 支持导出骑行数据 CSV
- 当前版本已经禁用手工上传与手工天气录入，数据页主要作为真实数据展示入口

### 3.4 48 小时预测服务

系统目前可以基于生产 LSTM 模型，对 62 个站点未来 48 小时净流量进行批量预测，也可以查看单个站点的预测结果。

实现方式：

- 核心服务在 [bike_dispatch_platform/demand_prediction/services/station_prediction_service.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/demand_prediction/services/station_prediction_service.py)
- 服务会读取：
  - 生产模型 `bike_dispatch_platform/demand_prediction/model_assets/bike_sharing_lstm_model.keras`
  - 生产缩放器 `bike_dispatch_platform/demand_prediction/model_assets/scaler.pkl`
  - 指标文件 `bike_dispatch_platform/demand_prediction/model_assets/model_metrics.json`
- 对每个站点取最近 48 小时特征窗口
- 运行模型后输出未来 48 小时净流量预测
- 再结合当前库存，推演未来 48 小时库存轨迹
- 推演过程中用 `_rebalance_vector` 严格保证全局 1200 辆守恒

### 3.5 预测页面与 API

系统提供了预测页面、结果页面和标准化预测 API。

实现方式：

- 视图在 [bike_dispatch_platform/demand_prediction/views.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/demand_prediction/views.py)
- 路由在 [bike_dispatch_platform/demand_prediction/urls.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/demand_prediction/urls.py)

当前可用接口包括：

- `GET /predict/api/48h/`
  - 返回全站 62 个站点的未来 48 小时预测结果
- `GET /predict/api/station/<station_id>/`
  - 返回单站点未来 48 小时净流量与库存轨迹
- `GET /predict/spot/api/`
  - 返回用于前端站点预测展示的批量预测结果

### 3.6 实时运行链路

系统当前支持把小时级预测结果转换成 10 秒级实时状态，用于首页、热力图和运维页面刷新。

实现方式：

- 核心逻辑在 [bike_dispatch_platform/operation_management/services/runtime_service.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/operation_management/services/runtime_service.py)
- 实时服务会按 10 秒分桶生成 `RuntimeSnapshot`
- 若当前 10 秒桶已有快照，则直接复用
- 若没有，则基于上一时刻库存和预测净流量进行插值更新
- 更新后会生成：
  - 每个站点的实时库存
  - 可用泊位
  - 预警状态
  - 未来 4 小时预测库存
  - 供需缺口

### 3.7 SQLite 锁冲突防护

系统已经处理了 `database is locked` 问题。

实现方式：

- 在 `runtime_service` 中加入了进程级 `_SNAPSHOT_LOCK`
- 同时增加了 10 秒桶的 `_SNAPSHOT_CACHE`
- 这样同一刷新周期内的快照生成会串行执行并复用结果
- 避免了 SQLite 在高频读写下的并发锁表问题

### 3.8 仪表盘与首页实时展示

系统首页已经可以展示：

- 平台总车辆数
- 在线站点数
- 当日累计骑行量
- 当前全局供需缺口
- 实时地图
- 站点状态
- 骑行趋势
- 未来 48 小时趋势
- 调度建议
- 系统设置摘要

实现方式：

- 数据由 [bike_dispatch_platform/system_support/services/dashboard_service.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/system_support/services/dashboard_service.py) 统一组装
- `dashboard_service` 会同时调用：
  - `runtime_service.ensure_snapshot()`
  - `station_prediction_service.get_batch_response()`
- 最终形成首页所需的 `dashboard_payload`
- 前端通过 10 秒轮询接口实现自动刷新

### 3.9 调度热力图与调度建议

系统可以展示供需热力图，并根据预测缺口尝试生成自动调度建议。

实现方式：

- 供需热力图页面在 `operation_management` 模块
- 调度建议在 `runtime_service` 中通过 `_transport_optimize` 生成
- 算法底层使用 `scipy.optimize.linprog` 求解运输问题
- 输入是盈余站点与缺口站点
- 输出是起点、终点、调运量、距离成本和原因说明
- 若当前不满足求解条件，则不会伪造调度建议

### 3.10 手动调度

系统支持人工创建调度任务。

实现方式：

- 逻辑在 [bike_dispatch_platform/operation_management/services/vehicle_service.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_dispatch_platform/operation_management/services/vehicle_service.py)
- 创建前会校验：
  - 起点和终点不能相同
  - 调运量必须大于 0
  - 全局车辆总数必须仍是 1200
  - 起点库存足够
  - 终点容量足够
- 通过后创建 `ScheduleTask`

### 3.11 车辆运维、故障上报与工单

系统已具备车辆级运维管理能力。

实现方式：

- `vehicle_service.ensure_vehicle_registry()` 会根据当前实时快照，确保系统中有且仅有 1200 辆车辆记录
- 若车辆记录数不对，会自动重建到 1200 辆
- 故障上报功能会将车辆状态切为 `faulty`
- 同时生成故障工单或运维工单
- 运维页面还能展示最新骑行记录样本

### 3.12 站点运维管理

系统支持查看和编辑站点运维字段。

实现方式：

- 站点列表页会读取主数据与实时快照拼接后的结果
- 可以查看站点基础信息和实时运行信息
- 当前开放编辑的是非核心运维字段，例如：
  - 低/高库存预警阈值
  - 备注
  - 启用状态
- 核心字段如站点编号、名称、经纬度、容量、映射关系并不通过运维页面修改

### 3.13 历史数据导出

系统支持按站点导出历史数据。

实现方式：

- 站点历史导出在 `operation_management` 里实现
- 可结合预测结果或历史数据视图进行 CSV 导出

### 3.14 系统设置、备份和日志

系统具备基础系统支撑能力。

实现方式：

- 系统设置页可修改刷新频率、预警阈值、调度阈值、模型版本显示等运行参数
- 备份功能会直接复制数据库文件，形成带时间戳的备份文件
- 系统日志会记录登录、退出、备份、设置修改、调度等关键操作

## 4. 项目当前的业务闭环是怎么跑通的

项目当前的核心闭环如下：

1. 读取 `ysu_62_stations_hourly_core_dataset.csv`
2. 为每个站点取最近 48 小时数据
3. 通过生产 LSTM 模型得到未来 48 小时净流量预测
4. 基于预测净流量推演未来库存轨迹
5. 严格执行 1200 辆全局守恒
6. 生成 10 秒级实时快照
7. 计算每个站点的供需缺口和预警状态
8. 在满足条件时生成调度建议
9. 通过仪表盘、热力图、站点页和运维页展示给用户

也就是说，项目现在已经不是单独的“预测模型演示”，而是一套带真实映射数据、预测服务、实时展示、调度分析和运维支撑的完整工程系统。

## 5. 根目录当前还额外具备什么独立能力

除了生产系统本体，项目根目录还保留了独立的模型研究与效果验证能力，主要用于毕设分析，不会直接影响生产环境：

- [build_lstm_system.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/build_lstm_system.py)
  - 生产模型训练脚本
- [build_lstm_system_optimized.py](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/build_lstm_system_optimized.py)
  - 独立优化版训练脚本
- [bike_sharing_lstm_model_optimized.keras](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/bike_sharing_lstm_model_optimized.keras)
  - 根目录独立导出的优化版模型
- [optimized_feature_scaler.pkl](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/optimized_feature_scaler.pkl)
  - 优化版缩放器
- [LSTM模型优化核心指标对比报告.md](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/LSTM模型优化核心指标对比报告.md)
  - 原版、优化版、BP 基准的指标对比
- [LSTM模型预测效果可视化分析报告.md](/e:/develop/BSDP-Bike%20Sharing%20Demand%20Prediction%20Based%20on%20LSTM%20Model/BSDP/LSTM模型预测效果可视化分析报告.md)
  - 答辩用可视化分析材料

这些内容主要服务于“模型优化验证”和“毕设答辩展示”，不会自动替换生产模型。

## 6. 当前项目的状态总结

从当前项目状态看，它已经能稳定实现以下几件事：

- 用真实映射数据驱动系统运行
- 对 62 个燕山大学站点做未来 48 小时预测
- 将预测结果转换为实时库存、缺口、预警与调度展示
- 在前端以 10 秒刷新方式稳定展示
- 支持人工调度、站点运维、车辆运维、故障工单、系统设置、备份和日志
- 通过根目录独立脚本和报告继续做模型效果验证与答辩材料输出

如果从毕设角度概括，这个项目当前已经具备“数据真实、预测可用、业务闭环完整、可视化可展示、系统可运维”的完整交付能力。
